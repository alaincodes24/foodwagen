export type Food = {
  createdAt: string;
  name: string;
  avatar: string;
  open: boolean;
  logo: string;
  id: string;
  Price: `${number}`;
  image: string;
  rating: number;
  restaurantName: string;
  status: string;
};
