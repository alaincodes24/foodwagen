export type Food = {
  createdAt: string;
  name: string;
  avatar: string;
  open: boolean;
  logo: string;
  id: string;
  Price: `${number}`;
  image: string;
  rating: string;
  restaurantName: string;
  status: string;
};
